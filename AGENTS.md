# AGENTS.md

## プロジェクト概要

- プロジェクト名: `minesweeper-on-next`
- 構成: Next.js 15 + React 19 + TypeScript
- パッケージマネージャ/ランタイム: Bun（`mise.toml` で管理）
- 主機能: マインスイーパー（難易度切替、地雷配置、開閉、旗、勝敗判定）

## セットアップ/実行

- 初回セットアップ
  - `mise install`
  - `bun install`
- 開発サーバー: `bun run dev`
- ビルド: `bun run build`
- 本番起動: `bun run start`
- 静的解析: `bun run lint`

## 主要ファイル

- `app/page.tsx`
  - クライアント側 UI とゲーム進行（状態管理、入力イベント、表示）
- `lib/minesweeper.ts`
  - ゲームロジックの中核（盤面生成、地雷配置、セル開放、旗操作、勝敗判定）

## 実装ルール（このリポジトリ向け）

- 変更は最小限にし、既存 API と型定義を優先して維持する。
- ゲームルールの変更は `lib/minesweeper.ts` に集約し、UI 層へロジックを分散させない。
- UI の文言/操作仕様（タップで開く、長押し/右クリックで旗）は既存挙動との整合を保つ。
- TypeScript の型を明示し、`any` の追加は避ける。
- 依存追加は原則行わず、必要性が明確な場合のみ最小限で行う。
- Pull Request（PR）のタイトル・本文・コメントは日本語で記載する。
- UI 変更がある PR では、変更前/変更後のスクリーンショットを PR 本文に貼り付ける（GitHub の画像貼り付け機能を使用）。

## 動作確認チェックリスト

- 難易度切替後に盤面サイズ/地雷数が設定どおり反映される。
- 初手で地雷を踏まない（初回オープン座標は安全）。
- 地雷セルを開いた時に敗北状態になり、地雷が表示される。
- 非地雷セルをすべて開いた時に勝利状態になる。
- 旗の付与/解除で残り地雷表示が追従する。
- `bun run lint` と `bun run build` が通る。

## 補足

- 現在、専用のテストフレームワーク設定は含まれていないため、変更時は上記チェックリストによる手動確認と `lint/build` 実行を基本とする。
